//cargo == nuget/pip/npm
//cargo --version


//to create a new project with CLI
//cargo new "name"


//TOML (Tom’s Obvious, Minimal Language)
//crates == packages


//build cargo with
//cargo build


//cargo or cargo -h command for help

//target dir is created with cargo build
//in the target folder, is a debug folder with a  "cargo_name" executable

//to execute
//cargo run 

//cargo check
//to verify it compiles without building

//cargo build --release
//like cargo build, creates a folder in target, named release